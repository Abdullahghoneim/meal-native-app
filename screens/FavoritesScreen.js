import React from "react";
import { Text, View, StyleSheet, FlatList } from "react-native";

const FavoritesScreen = props => {
  return (
    <View>
      <Text>Favirot screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default FavoritesScreen;
